---
const { startX, startY, endX, endY, strokeColor, strokeWidth } = Astro.props;
---

<line
  x1={startX}
  y1={startY}
  x2={endX}
  y2={endY}
  stroke={strokeColor}
  stroke-width={strokeWidth}
/>
